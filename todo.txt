дэ = дата экспирации (expDt)
гдз = гарантированная дата завершения = (expDt.year, expDt.moth, 1).AddMonth(1);
// guarExpDt   = guaranteed expiration date
// guarStartDt = guaranteed start date - гарантированная дата начала торговли


загружать можем только завершенные фьючи, т.е. фьючи у которых наступила guarExpDt 

- объединение скаченных по фьючам данных в файлы (например, BR-7.19)
	пока не доступно
	можно сделать, но пока пропущу

- загрузка инструмента по id ()
	доступно только для акций и фьючерсов
 

 

АКЦИИ

- обновление данных по акциям (например, SBER)
	читаем файл с датами
		определяем крайнюю дату
		
		проверяем, скачаны ли файлы за даты (может присутствовать / отсутствовать)
		если не скачаны - скачиваем
		
	запрашиваем дневные свечи с крайнюю даты + 1 день, до вчера
		дневки - дописываем в файл
		скачиваем файлы за даты


- загрузка данных за период по акциям (например, SBER)
	dtBeg = (y1, m1, d1);
	dtEnd = (y2, m2, d2);
	
	запрашиваем дневки за этот период
	дневки - НЕ дописываем в файл
	скачиваем файлы за даты
